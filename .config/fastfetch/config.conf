# ~/.config/fastfetch/config.conf

--logo-color=2:10:2        # Dark green logo (RGB)
--title-color=2:10:2       # Dark green text
--separator-color=2:10:2   # Green separator (optional)

# Optional: Use a different logo (e.g., "nixos" or "arch")
--logo=nixos

# Disable elements you don’t want
--disable=model
--disable=resolution
